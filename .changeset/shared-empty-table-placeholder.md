---
'@journeyapps/reactor-mod': patch
---

Export EmptyTableWidget and display the empty state inside the base table border, including searchable and multiselect tables.

Avoid doubling the border between the table header and the first group row.
